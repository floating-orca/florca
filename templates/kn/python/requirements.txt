parliament-functions==0.1.0

# Transitive dependencies of parliament-functions, which declares them unversioned.
# cloudevents 2.0 removed the cloudevents.http module that parliament imports.
cloudevents==1.13.0
flask==3.1.3
waitress==3.0.2
